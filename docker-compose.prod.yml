version: '3'
services:
  backend:
    container_name: node-docker-ci-cd
    build:
      context: .
      args:
        - NODE_ENV=production
      dockerfile: Dockerfile
    image: node-docker-ci-cd
    ports:
      - 3010:${APP_PORT}
    volumes:
      - ./src:/app/src
    env_file:
      - ./.env
    environment:
      - NODE_ENV=production
      - APP_PORT=${APP_PORT}
    